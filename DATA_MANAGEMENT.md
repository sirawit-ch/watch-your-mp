# 📊 Data Management Guide

## วิธีการจัดการข้อมูล

โปรเจกต์นี้แยกการ **ดึงข้อมูล** ออกจาก **การ deploy** เพื่อประหยัดเวลาและทรัพยากร

---

## 🔄 วิธีที่ 1: GitHub Actions (อัตโนมัติ - แนะนำ)

### การตั้งค่า

ไฟล์ `.github/workflows/update-data.yml` จะดึงข้อมูลอัตโนมัติตามเวลาที่กำหนด

### กำหนดเวลาดึงข้อมูล

แก้ไขบรรทัด `cron` ใน `.github/workflows/update-data.yml`:

```yaml
schedule:
  # ตัวอย่าง cron schedules:
  - cron: "0 0 * * *" # ทุกวันเที่ยงคืน (00:00 UTC)
  - cron: "0 */6 * * *" # ทุก 6 ชั่วโมง
  - cron: "0 0 * * 1" # ทุกวันจันทร์
  - cron: "0 9 * * 1-5" # ทุกวันจันทร์-ศุกร์ เวลา 09:00 UTC
```

### การทำงาน

1. GitHub Actions จะรัน script ดึงข้อมูลตามเวลาที่กำหนด
2. ข้อมูลใหม่จะถูก commit และ push กลับมาที่ repository
3. GitHub Pages จะ re-deploy อัตโนมัติเมื่อมี commit ใหม่

### ดึงข้อมูลแบบ Manual

ไปที่ GitHub → Actions → "Update Data" → Run workflow

---

## 💻 วิธีที่ 2: Local Manual Update

### ดึงข้อมูลใหม่

```bash
npm run data:update
```

### ดึงข้อมูล + Build

```bash
npm run data:update-and-build
```

### Commit และ Push

```bash
git add public/data/
git commit -m "chore: update data"
git push
```

---

## 📦 วิธีที่ 3: Deploy แบบแยกส่วน

### 1. แก้ไข Build Script (ถ้าต้องการ)

ปัจจุบัน `npm run build` จะ build เฉพาะ Next.js **ไม่ดึงข้อมูล**

### 2. การ Deploy

```bash
# Deploy โดยไม่ดึงข้อมูลใหม่
npm run build

# หรือถ้าต้องการดึงข้อมูลก่อน deploy
npm run data:update-and-build
```

---

## 🎯 การใช้งานที่แนะนำ

### สำหรับ Production

✅ **ใช้ GitHub Actions (วิธีที่ 1)** - ดึงข้อมูลอัตโนมัติตามเวลา

### สำหรับ Development

✅ **ใช้ข้อมูลเก่า** - ใช้ข้อมูลที่มีอยู่แล้วใน `public/data/`
✅ **ดึงข้อมูลเมื่อจำเป็น** - รัน `npm run data:update` เมื่อต้องการข้อมูลล่าสุด

---

## 📝 ตัวอย่าง Workflow

### Scenario 1: แก้โค้ด UI/UX

```bash
# ไม่ต้องดึงข้อมูลใหม่
npm run dev      # develop
npm run build    # build
git push         # deploy
```

### Scenario 2: ต้องการข้อมูลล่าสุด

```bash
npm run data:update              # ดึงข้อมูล
git add public/data/
git commit -m "chore: update data"
git push                         # deploy
```

### Scenario 3: อัตโนมัติ (แนะนำ)

- GitHub Actions จะดึงข้อมูลอัตโนมัติตาม schedule
- ไม่ต้องทำอะไรเลย! 🎉

---

## ⚙️ Scripts ที่มีให้ใช้

| Script                          | คำอธิบาย                     |
| ------------------------------- | ---------------------------- |
| `npm run dev`                   | รัน development server       |
| `npm run build`                 | Build Next.js (ไม่ดึงข้อมูล) |
| `npm run data:update`           | ดึงข้อมูลใหม่จาก GraphQL     |
| `npm run data:update-and-build` | ดึงข้อมูล + Build            |

---

## 🔍 ตรวจสอบข้อมูล

### ดูว่าข้อมูลอัพเดทเมื่อไหร่

```bash
ls -la public/data/new-data/
```

### ดูขนาดไฟล์ข้อมูล

```bash
du -h public/data/new-data/
```

---

## 🚨 Troubleshooting

### ปัญหา: GitHub Actions ไม่รัน

- ตรวจสอบว่า workflow file อยู่ใน `.github/workflows/`
- ตรวจสอบ permissions ใน Settings → Actions → General

### ปัญหา: Python script ไม่ทำงาน

```bash
# ติดตั้ง dependencies
pip install requests
```

### ปัญหา: ข้อมูลไม่อัพเดท

- ตรวจสอบ GitHub Actions logs
- รัน `npm run data:update` locally เพื่อดู error
